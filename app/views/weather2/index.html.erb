<h1>Local Weather</h1>
<%= form_with(url: { controller: 'weather2', action: 'retrieve' }, local: true) do |form| %>
  <div class="field">

    <%# I borrowed this prompt verbiage from https://www.weather.gov/ %>
    <%= form.label 'Local forecast by "City, St" or ZIP code:' %><br />
    <%= form.text_field :location_text %>
  </div>

  <div class="actions">
    <%= form.submit 'Submit' %>
  </div>
<% end %>

